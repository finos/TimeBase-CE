package com.epam.deltix.qsrv.hf.tickdb;

import com.epam.deltix.qsrv.hf.tickdb.pub.TickDBFactory;

public class StreamsStreamReader {

    public static void main(String[] args) {
//        try (DXTickDB db = TickDBFactory.openFromUrl("dxtick://localhost:8011", false)) {
//            try (TickCursor cursor = db.select(Long.MIN_VALUE, new SelectionOptions(), db.getStream("streams"))) {
//                while (cursor.next()) {
//                    System.out.println(cursor.getMessage());
//                }
//            }
//        }
    }

}
